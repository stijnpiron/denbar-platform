export enum PermissionResource {
  ALLIANCES = 'alliances',
  GROUPS = 'groups',
  ORDERS = 'orders',
  PERIODS = 'periods',
  PRODUCTS = 'products',
  PROFILES = 'profiles',
  USERS = 'users',
  YEARS = 'years',
}
